# Scripts

This folder will contain repeatable, non-secret maintenance scripts:

- `seed_chroma.py` — load approved backlog records into the local vector store.
- `run_local.ps1` — start the FastAPI backend and React client for demos.

Scripts are added in the next implementation phase after the tool adapters exist.
