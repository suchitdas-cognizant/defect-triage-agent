# Local data

`data/` stores non-secret development fixtures and the local ChromaDB directory.
The `data/chroma/` directory is generated locally and must not be uploaded with
the hackathon submission. Seed records will be introduced through a script so
the same knowledge base can be reproduced on another machine.
